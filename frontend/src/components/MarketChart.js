import React from 'react';

const MarketChart = ({ data }) => {
  return (
    <section id="chart-section">
      <h2>Market Chart</h2>
      <div id="trading-chart">
        <p>Chart will be displayed here (lightweight-charts integration pending)</p>
      </div>
    </section>
  );
};

export default MarketChart;
